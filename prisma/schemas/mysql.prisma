generator client {
  provider        = "prisma-client-js"
  previewFeatures = ["tracing"]
  output          = "../clients/mysql"
}

datasource db {
  provider = "mysql"
  url      = ""
}

model User {
  id String @id @default(cuid())
}

generator client {
  provider = "prisma-client-js"
  output   = "../clients/postgres"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  id String @id @default(cuid())
}

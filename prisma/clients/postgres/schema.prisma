generator client {
  provider        = "prisma-client-js"
  previewFeatures = ["tracing"]
  output          = "../clients/postgres"
}

datasource db {
  provider = "postgresql"
  url      = ""
}

model User {
  id String @id @default(cuid())
}
